#include "main.h"
/**
 * print_last_digit - print last digit of integer
 * @int: integer
 * Return: last digit of number
 */
int _abs(int)
{
if (int < 0)
{
return (int * -1);
}
else
{
return (int);
}
}
